import { HighlightedNavItemsDirective } from './highlighted-nav-items.directive';

describe('HighlightedNavItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightedNavItemsDirective();
    expect(directive).toBeTruthy();
  });
});
