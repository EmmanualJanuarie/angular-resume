import { HamburgerBarHighlightDirective } from './hamburger-bar-highlight.directive';

describe('HamburgerBarHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HamburgerBarHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
